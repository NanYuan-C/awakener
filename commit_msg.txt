refactor: reorganize project into layered architecture

- Replace activator/ + server/ monoliths with core/, services/, agents/, api/ layered structure
- Centralize LLM client, logger, config into core/
- Extract tool registry (agents/tools/) with shell, file, executor, stealth modules
- Split agents into activator/ (loop) and auditor/ (snapshot), stub chat/
- Move prompt templates to templates/{prompts,home,zh-CN}/ with language selection on first run
- Move persona.md and rules.md into agents/activator/ (each agent owns its prompts)
- Add browser-driven language detection via POST /api/init/language
- Rename config key language to template_language
- Remove community feature from config
- Add CHAT_AGENT_DESIGN.md as blueprint for future chat agent
